# -*- coding: utf-8 -*-
"""Linear Regression Mutiple variables.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1MuPiG54LVFmp5qdysxaaYkZU-RIyWBrM
"""

import pandas as pd
import numpy as np
from sklearn import linear_model

df = pd.read_csv('homeprices.csv')
df

import math
median_bedrooms = math.floor(df.bedrooms.median())
median_bedrooms

df.bedrooms = df.bedrooms.fillna(median_bedrooms)
df

#training the model

reg = linear_model.LinearRegression()
reg.fit(df[['area', 'bedrooms', 'age']], df.price)

reg.coef_

reg.intercept_

reg.predict([[3000,3,40]])

# this is how it is calculated
112.06244194*3000+23388.88007794*3+-3231.71790863*40+221323.00186540396

# this room is very new so the price of the home will be more because it has 5 years
reg.predict([[2500,4,5]])

"""# Excercise"""

!pip install word2number

import pandas as pd
import numpy as np
from sklearn import linear_model
from word2number import w2n

d = pd.read_csv('hiring.csv')
d

d.experience = d.experience.fillna("zero")
d

d.experience = d.experience.apply(w2n.word_to_num)
d

import math
median_test_score = math.floor(d['test_score(out of 10)'].mean())
median_test_score

d['test_score(out of 10)'] = d['test_score(out of 10)'].fillna(median_test_score)
d

reg = linear_model.LinearRegression()
reg.fit(d[['experience','test_score(out of 10)','interview_score(out of 10)']],d['salary($)'])

reg.predict([[2,9,6]])

reg.predict([[12,10,10]])